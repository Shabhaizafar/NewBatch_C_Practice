#include<stdio.h>
#include<string.h>

void main(){
    // int numarr[] = {11,12,13};
    // char chararr[] = {'a','b','c'};
    // float fltarr[] = {3.14,2.303};

    // char str1[] ="royal";  //string


    //string array 1D

    // char strarr[4][10] = {"Royal","Technosoft","pvt","Ltd"};
    // char strarr[][10] = {"Royal","Technosoft","pvt","Ltd"};
    // char strarr[3][10] = {"Royal","Technosoft","pvt","Ltd"};
    char strarr[][10] = {"Royal","Technosoft","pvt","Ltd"};

// 
    // printf("Size of Array : %d",sizeof(strarr));

    // printf("Length/element of Array : %d",sizeof(strarr)/sizeof(strarr[0]));
    // printf("Length/element of Array : %d",sizeof(strarr)/10);

    // printf("String Array is : ");
    // for (int i = 0; i < sizeof(strarr)/sizeof(strarr[0]); i++)
    // {
    //     printf("%s  ",strarr[i]);
    // }
    
}


/*
    🟢 Easy Level

    Input and display 5 names using an array of strings.

    Find the longest string among given n strings.

    Sort an array of strings in alphabetical order (dictionary order).

    Search for a string in an array of strings using linear search.

    🟡 Intermediate Level

    Count how many strings start with a vowel (A, E, I, O, U).

    Check if two strings in the array are anagrams (e.g., "listen" and "silent").

    Concatenate all strings in the array into one big string.

    Find the frequency of each word in an array of strings.

    🔴 Hard Level

    Remove duplicate strings from an array of strings (keep only unique ones).

    Sort strings by their length (shortest to longest).

*/

//char str1 ="royal";